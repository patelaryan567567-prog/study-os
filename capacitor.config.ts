import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.studyos.app",
  appName: "StudyOS",
  webDir: "dist",
  bundledWebRuntime: false,
  plugins: {
    // plugin-specific config can go here
  },
};

export default config;
